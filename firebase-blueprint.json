{
  "entities": {
    "WaitlistEntry": {
      "title": "WaitlistEntry",
      "description": "A user's email registered for the waitlist.",
      "type": "object",
      "properties": {
        "email": {
          "type": "string",
          "format": "email",
          "description": "The email address of the user."
        },
        "createdAt": {
          "type": "string",
          "format": "date-time",
          "description": "The time the user joined the waitlist."
        }
      },
      "required": ["email", "createdAt"]
    }
  },
  "firestore": {
    "/waitlist/{entryId}": {
      "schema": "WaitlistEntry",
      "description": "Collection of waitlist signups."
    }
  }
}
